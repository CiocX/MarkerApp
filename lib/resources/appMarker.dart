class AppMarker {
  String id;
  String title;
  String author;
  String category;
  String description;
  DateTime startDate;
  DateTime endDate;
  double latitude;
  double longitude;

  AppMarker (this.id, this.title, this.author, this.category, this.description, this.startDate, this.endDate, this.latitude, this.longitude);
}